/**
 * @example 
 * ```
 * jpx.config.logPerformance = true;
 * myImage.sharpen();
 * // [jpx] sharpen()      - 80ms
 * ```
 */
export const config = {
    /**
     * If true, log the performance in the console
     */
    logPerformances: false
};